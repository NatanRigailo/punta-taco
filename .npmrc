# Install scripts are the single biggest npm supply-chain vector: a compromised
# package runs arbitrary code on install, before anyone reads a line of it.
# Nothing here needs them.
ignore-scripts=true

# No implicit network beyond the registry, and no telemetry noise in CI logs.
audit=true
fund=false
